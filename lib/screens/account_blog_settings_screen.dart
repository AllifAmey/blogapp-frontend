import 'package:flutter/material.dart';

class AccountBlogSettings extends StatelessWidget {
  // Account settings for Blog
  const AccountBlogSettings({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
